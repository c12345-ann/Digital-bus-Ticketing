import { NextResponse } from "next/server";
import { createClient } from "@/lib/supabase/server";
import { createAdminClient } from "@/lib/supabase/admin";
import { emailPattern, isUserRole, validatePassword } from "@/lib/validation";

async function requireAdmin(){const supabase=await createClient();const{data}=await supabase.auth.getClaims();const id=data?.claims?.sub;if(!id)return null;const{data:profile}=await supabase.from("profiles").select("role").eq("id",id).single();return profile?.role==="administrator"?id:null;}
export async function POST(request){if(!await requireAdmin())return NextResponse.json({message:"Forbidden."},{status:403});const b=await request.json().catch(()=>({}));const email=String(b.email??"").trim().toLowerCase(),password=String(b.password??"");if(!emailPattern.test(email)||!isUserRole(b.role)||validatePassword(password)||!String(b.firstName??"").trim()||!String(b.lastName??"").trim())return NextResponse.json({message:"Enter valid user details and a strong temporary password."},{status:400});try{const admin=createAdminClient();const{data,error}=await admin.auth.admin.createUser({email,password,email_confirm:true,user_metadata:{first_name:String(b.firstName).trim(),last_name:String(b.lastName).trim(),phone:String(b.phone??"").trim()}});if(error)return NextResponse.json({message:error.message},{status:400});const{error:profileError}=await admin.from("profiles").update({role:b.role}).eq("id",data.user.id);if(profileError){await admin.auth.admin.deleteUser(data.user.id);return NextResponse.json({message:profileError.message},{status:400});}return NextResponse.json({user:data.user});}catch(error){return NextResponse.json({message:error instanceof Error?error.message:"Unable to create user."},{status:500});}}
export async function PATCH(request){if(!await requireAdmin())return NextResponse.json({message:"Forbidden."},{status:403});const b=await request.json().catch(()=>({}));const status=b.status==="Suspended"?"Suspended":"Active";try{const admin=createAdminClient();const{error:authError}=await admin.auth.admin.updateUserById(String(b.id),{ban_duration:status==="Suspended"?"876000h":"none"});if(authError)return NextResponse.json({message:authError.message},{status:400});const{error}=await admin.from("profiles").update({account_status:status}).eq("id",b.id);if(error)return NextResponse.json({message:error.message},{status:400});return NextResponse.json({ok:true});}catch(error){return NextResponse.json({message:error instanceof Error?error.message:"Unable to update user."},{status:500});}}
